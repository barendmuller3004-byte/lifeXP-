{
  "name": "canopy-push-server",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal Web Push backend for Canopy reminders — the piece that lets a reminder reach you even when the app/browser is fully closed.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "generate-vapid-keys": "web-push generate-vapid-keys"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "web-push": "^3.6.7"
  }
}
